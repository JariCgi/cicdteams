name: CI/CD (multi-item RSS under /feed/feed.xml, bash-only)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      OWNER: ${{ github.repository_owner }}   # esim. jaricgi
      REPO:  ${{ github.repository }}         # esim. JariCgi/cicdteams
      SHA:   ${{ github.sha }}
      ACTOR: ${{ github.actor }}
      COMMIT_MSG: ${{ github.event.head_commit.message || 'Automated deploy' }}
      KEEP_COUNT: "10"                        # montako viimeistä itemiä pidetään feedissä

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build site (index.html)
        shell: bash
        run: |
          set -euo pipefail
          REPO_NAME="${REPO#*/}"
          BASE_URL="https://${OWNER}.github.io/${REPO_NAME}/"
          mkdir -p dist
          cat > dist/index.html <<'HTML'
          <!doctype html>
          <html lang="fi">
            <head>
              <meta charset="utf-8" />
              <meta name="viewport" content="width=device-width,initial-scale=1" />
              <title>CI/CD demo</title>
              <style>
                body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;margin:2rem;line-height:1.5}
                code{background:#f4f4f4;padding:.2rem .4rem;border-radius:.25rem}
                a{color:#0366d6;text-decoration:none}
                a:hover{text-decoration:underline}
              </style>
            </head>
            <body>
              <h1>CI/CD demo</h1>
              <p><strong>Päivitetty:</strong> __DATE__</p>
              <p>RSS: <a href="./feed/feed.xml">./feed/feed.xml</a></p>
            </body>
          </html>
          HTML
          sed -i "s|__DATE__|$(date -u)|" dist/index.html

      - name: Generate multi-item RSS (/feed/feed.xml) by merging existing feed
        shell: bash
        run: |
          set -euo pipefail

          REPO_NAME="${REPO#*/}"
          BASE_URL="https://${OWNER}.github.io/${REPO_NAME}/"
          mkdir -p dist/feed

          NOW_RFC="$(date -u +"%a, %d %b %Y %H:%M:%S GMT")"
          GUID="${SHA}-$(date +%s)"

          # XML-escape otsikko
          ESC_TITLE="$(printf '%s' "${COMMIT_MSG:-Automated deploy}" \
